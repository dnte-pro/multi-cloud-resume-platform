# Objective:
Build a production-style personal portfolio where:

- Frontend is hosted on Azure Blob Storage + Azure CDN
- Backend API runs on AWS ECS (we'll start with ECS before EKS)
- Visitor analytics are stored in DynamoDB
- Infrastructure is provisioned with Terraform
- CI/CD is automated with GitHub Actions
- Monitoring uses CloudWatch and Azure Monitor