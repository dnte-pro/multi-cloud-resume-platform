# Leave unset locally: the frontend calls FastAPI at http://127.0.0.1:8000.
# In production, set this to the deployed FastAPI origin (no trailing slash),
# unless your web server routes /api to FastAPI on the same domain.
# VITE_API_URL=https://api.example.com
